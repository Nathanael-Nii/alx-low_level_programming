#include <stdio.h>

/**
 * main - this function sends formatted output to stdout
 *
 * Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
